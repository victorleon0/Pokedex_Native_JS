pokemons.forEach(pokemon => {
    ALL_POKEMONS_INFO.push(pokemon);
    console.log(ALL_POKEMONS_INFO)
    
});


const cardImg = pokedex.createElement("img");
        
        const cardDiv = pokedex.createElement("div");
        const cardTitle = pokedex.createElement("h2");
        h2.innerText = (pokemon.name)
        const cardSubtitle = document.createElement("p")


        pokedex.appendChild(cardDiv)
        div.appendChild(cardTitle)
        div.appendChild(cardSubtitle)
        div.appendChild(cardImg)